// DESCRIPTION: Verilator output: Waivers generated with --waiver-output

`verilator_config

// Below are suggested waivers. You have three options:
//   1. Fix the reason for the linter warning in the Verilog sources
//   2. Keep the waiver permanently if you are sure it is okay
//   3. Keep the waiver temporarily to suppress the output

// lint_off -rule PINMISSING -file "*/foss/designs/src/counter.sv" -match "Instance has missing pin: 'VDD'*"

// lint_off -rule PINMISSING -file "*/foss/designs/src/counter.sv" -match "Instance has missing pin: 'VSS'*"

// lint_off -rule IMPLICIT -file "*/foss/designs/src/counter.sv" -match "Signal definition not found, creating implicitly: 'din'*"

// lint_off -rule IMPLICIT -file "*/foss/designs/src/counter.sv" -match "Signal definition not found, creating implicitly: 'dout'*"

// lint_off -rule WIDTHEXPAND -file "*/foss/designs/src/counter.sv" -match "Input port connection 'A' expects 6 bits on the pin connection, but pin connection's VARREF 'cnt' generates 4 bits.*"

// lint_off -rule WIDTHEXPAND -file "*/foss/designs/src/counter.sv" -match "Input port connection 'D' expects 8 bits on the pin connection, but pin connection's VARREF 'din' generates 1 bits.*"

// lint_off -rule WIDTHEXPAND -file "*/foss/designs/src/counter.sv" -match "Output port connection 'Q' expects 8 bits on the pin connection, but pin connection's VARREF 'dout' generates 1 bits.*"

// lint_off -rule UNDRIVEN -file "*/foss/designs/src/counter.sv" -match "Signal is not driven: 'din'*"

// lint_off -rule UNUSEDSIGNAL -file "*/foss/designs/src/counter.sv" -match "Signal is not used: 'dout'*"

