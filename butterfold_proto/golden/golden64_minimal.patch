--- a/golden/gen_all_mode_vectors.py
+++ b/golden/gen_all_mode_vectors.py
@@ -5,18 +5,18 @@
 
 import numpy as np
 
-N = 128
+N = 64
 FRAC_BITS = 7
 SCALE = 1 << FRAC_BITS
 
 NUM_TWO_POINT_TESTS = 8
-NUM_FFT128_TESTS = 5
-NUM_IFFT128_TESTS = 5
+NUM_FFT64_TESTS = 5
+NUM_IFFT64_TESTS = 5
 SEED = 20260805
 
 CMD_FFT2 = 0x40
-CMD_FFT128 = 0x41
-CMD_IFFT128 = 0x42
+CMD_FFT64 = 0x41
+CMD_IFFT64 = 0x42
 CMD_IFFT2 = 0x43
 
 VECTOR_DIR = Path("vectors")
@@ -39,10 +39,10 @@
     return np.clip(scaled, -128, 127).astype(np.int16)
 
 
-def bit_reverse7(value: int) -> int:
+def bit_reverse6(value: int) -> int:
     result = 0
-    for bit in range(7):
-        result |= ((value >> bit) & 1) << (6 - bit)
+    for bit in range(6):
+        result |= ((value >> bit) & 1) << (5 - bit)
     return result
 
 
@@ -56,8 +56,8 @@
         twiddle_im[k] = max(-128, min(127, round(math.sin(angle) * SCALE)))
 
     with (
-        (VECTOR_DIR / "fft128_twiddle_re.hex").open("w", encoding="utf-8") as re_file,
-        (VECTOR_DIR / "fft128_twiddle_im.hex").open("w", encoding="utf-8") as im_file,
+        (VECTOR_DIR / "fft64_twiddle_re.hex").open("w", encoding="utf-8") as re_file,
+        (VECTOR_DIR / "fft64_twiddle_im.hex").open("w", encoding="utf-8") as im_file,
     ):
         for re, im in zip(twiddle_re, twiddle_im, strict=True):
             re_file.write(hex_signed(int(re), 8) + "\n")
@@ -127,7 +127,7 @@
     return result
 
 
-def transform128_model(
+def transform64_model(
     input_i: np.ndarray,
     input_q: np.ndarray,
     twiddle_re_rom: np.ndarray,
@@ -138,11 +138,11 @@
     ram_q = np.zeros(N, dtype=np.int64)
 
     for sample_index in range(N):
-        address = bit_reverse7(sample_index)
+        address = bit_reverse6(sample_index)
         ram_i[address] = wrap_signed(int(input_i[sample_index]), 16)
         ram_q[address] = wrap_signed(int(input_q[sample_index]), 16)
 
-    for stage in range(7):
+    for stage in range(6):
         half_size = 1 << stage
         group_size = 2 << stage
 
@@ -150,7 +150,7 @@
             for j in range(half_size):
                 addr0 = group_base + j
                 addr1 = addr0 + half_size
-                twiddle_index = j << (6 - stage)
+                twiddle_index = j << (5 - stage)
 
                 x0_i = int(ram_i[addr0])
                 x0_q = int(ram_q[addr0])
@@ -189,8 +189,8 @@
                 ram_q[addr1] = X1_q
 
     if inverse:
-        ram_i = np.asarray([wrap_signed(int(value) >> 7, 16) for value in ram_i])
-        ram_q = np.asarray([wrap_signed(int(value) >> 7, 16) for value in ram_q])
+        ram_i = np.asarray([wrap_signed(int(value) >> 6, 16) for value in ram_i])
+        ram_q = np.asarray([wrap_signed(int(value) >> 6, 16) for value in ram_q])
 
     return ram_i.astype(np.int16), ram_q.astype(np.int16)
 
@@ -211,7 +211,7 @@
     return tests
 
 
-def make_fft128_tests(rng: np.random.Generator):
+def make_fft64_tests(rng: np.random.Generator):
     tests: list[tuple[np.ndarray, np.ndarray]] = []
 
     impulse_i = np.zeros(N, dtype=np.int16)
@@ -227,7 +227,7 @@
     tone = 0.5 * np.exp(1j * 2.0 * math.pi * 7 * n / N)
     tests.append((q17(tone.real), q17(tone.imag)))
 
-    while len(tests) < NUM_FFT128_TESTS:
+    while len(tests) < NUM_FFT64_TESTS:
         tests.append((
             q17(rng.uniform(-1.0, 1.0, size=N)),
             q17(rng.uniform(-1.0, 1.0, size=N)),
@@ -236,7 +236,7 @@
     return tests
 
 
-def make_ifft128_tests(rng: np.random.Generator):
+def make_ifft64_tests(rng: np.random.Generator):
     tests: list[tuple[np.ndarray, np.ndarray]] = []
 
     constant_i = np.full(N, 64, dtype=np.int16)
@@ -253,7 +253,7 @@
     one_bin_q[7] = 32
     tests.append((one_bin_i, one_bin_q))
 
-    while len(tests) < NUM_IFFT128_TESTS:
+    while len(tests) < NUM_IFFT64_TESTS:
         tests.append((
             q17(rng.uniform(-1.0, 1.0, size=N)),
             q17(rng.uniform(-1.0, 1.0, size=N)),
@@ -298,7 +298,7 @@
         numpy_file.write("test,index,reference_real,reference_imag,rtl_real,rtl_imag\n")
 
         for test_index, (input_i, input_q) in enumerate(tests):
-            output_i, output_q = transform128_model(
+            output_i, output_q = transform64_model(
                 input_i,
                 input_q,
                 tw_re,
@@ -350,16 +350,16 @@
     twiddle_re, twiddle_im = generate_twiddles()
 
     two_point_tests = make_two_point_tests(rng)
-    fft128_tests = make_fft128_tests(rng)
-    ifft128_tests = make_ifft128_tests(rng)
+    fft64_tests = make_fft64_tests(rng)
+    ifft64_tests = make_ifft64_tests(rng)
 
     write_two_point_vectors(two_point_tests)
-    write_block_vectors("fft128", fft128_tests, twiddle_re, twiddle_im, inverse=False)
-    write_block_vectors("ifft128", ifft128_tests, twiddle_re, twiddle_im, inverse=True)
+    write_block_vectors("fft64", fft64_tests, twiddle_re, twiddle_im, inverse=False)
+    write_block_vectors("ifft64", ifft64_tests, twiddle_re, twiddle_im, inverse=True)
 
     print(f"Generated {NUM_TWO_POINT_TESTS} mixed FFT2/IFFT2 tests")
-    print(f"Generated {NUM_FFT128_TESTS} FFT128 tests")
-    print(f"Generated {NUM_IFFT128_TESTS} IFFT128 tests")
+    print(f"Generated {NUM_FFT64_TESTS} FFT64 tests")
+    print(f"Generated {NUM_IFFT64_TESTS} IFFT64 tests")
 
 
 if __name__ == "__main__":
--- a/golden/gen_fft128_twiddles.py
+++ b/golden/gen_fft64_twiddles.py
@@ -1,7 +1,7 @@
 from pathlib import Path
 import math
 
-N = 128
+N = 64
 FRAC_BITS = 7
 SCALE = 1 << FRAC_BITS
 
@@ -18,8 +18,8 @@
 
 
 with (
-    (out_dir / "fft128_twiddle_re.hex").open("w", encoding="utf-8") as re_file,
-    (out_dir / "fft128_twiddle_im.hex").open("w", encoding="utf-8") as im_file,
+    (out_dir / "fft64_twiddle_re.hex").open("w", encoding="utf-8") as re_file,
+    (out_dir / "fft64_twiddle_im.hex").open("w", encoding="utf-8") as im_file,
 ):
     for k in range(N // 2):
         angle = -2.0 * math.pi * k / N
@@ -30,5 +30,5 @@
         re_file.write(hex8(re) + "\n")
         im_file.write(hex8(im) + "\n")
 
-print("Generated vectors/fft128_twiddle_re.hex")
-print("Generated vectors/fft128_twiddle_im.hex")
+print("Generated vectors/fft64_twiddle_re.hex")
+print("Generated vectors/fft64_twiddle_im.hex")
--- a/golden/gen_fft128_vectors.py
+++ b/golden/gen_fft64_vectors.py
@@ -5,18 +5,18 @@
 
 import numpy as np
 
-N = 128
+N = 64
 NUM_TESTS = 50
 SEED = 1234
 FRAC_BITS = 7
 SCALE = 1 << FRAC_BITS
 
 VECTOR_DIR = Path("vectors")
-INPUT_PATH = VECTOR_DIR / "fft128_inputs.hex"
-EXPECTED_PATH = VECTOR_DIR / "fft128_expected.hex"
-NUMPY_PATH = VECTOR_DIR / "fft128_numpy_reference.csv"
-TWIDDLE_RE_PATH = VECTOR_DIR / "fft128_twiddle_re.hex"
-TWIDDLE_IM_PATH = VECTOR_DIR / "fft128_twiddle_im.hex"
+INPUT_PATH = VECTOR_DIR / "fft64_inputs.hex"
+EXPECTED_PATH = VECTOR_DIR / "fft64_expected.hex"
+NUMPY_PATH = VECTOR_DIR / "fft64_numpy_reference.csv"
+TWIDDLE_RE_PATH = VECTOR_DIR / "fft64_twiddle_re.hex"
+TWIDDLE_IM_PATH = VECTOR_DIR / "fft64_twiddle_im.hex"
 
 
 def wrap_signed(value: int, bits: int) -> int:
@@ -38,10 +38,10 @@
     return np.clip(np.round(np.asarray(value) * SCALE), -128, 127).astype(np.int16)
 
 
-def bit_reverse7(value: int) -> int:
+def bit_reverse6(value: int) -> int:
     result = 0
-    for bit in range(7):
-        result |= ((value >> bit) & 1) << (6 - bit)
+    for bit in range(6):
+        result |= ((value >> bit) & 1) << (5 - bit)
     return result
 
 
@@ -54,7 +54,7 @@
                 values.append(wrap_signed(int(token, 16), 8))
 
     if len(values) != N // 2:
-        raise ValueError(f"Expected 64 entries in {path}, found {len(values)}")
+        raise ValueError(f"Expected 32 entries in {path}, found {len(values)}")
 
     return np.asarray(values, dtype=np.int64)
 
@@ -103,7 +103,7 @@
     )
 
 
-def fft128_rtl_model(
+def fft64_rtl_model(
     input_i: np.ndarray,
     input_q: np.ndarray,
     twiddle_re_rom: np.ndarray,
@@ -111,18 +111,18 @@
 ) -> tuple[np.ndarray, np.ndarray]:
     """Run the same bit-reversed-input, iterative radix-2 DIT schedule as RTL."""
     if input_i.shape != (N,) or input_q.shape != (N,):
-        raise ValueError("FFT128 model requires exactly 128 complex samples")
+        raise ValueError("FFT64 model requires exactly 64 complex samples")
 
     ram_i = np.zeros(N, dtype=np.int64)
     ram_q = np.zeros(N, dtype=np.int64)
 
     # Scheduler writes natural-order input into bit-reversed RAM addresses.
     for sample_index in range(N):
-        address = bit_reverse7(sample_index)
+        address = bit_reverse6(sample_index)
         ram_i[address] = wrap_signed(int(input_i[sample_index]), 16)
         ram_q[address] = wrap_signed(int(input_q[sample_index]), 16)
 
-    for stage in range(7):
+    for stage in range(6):
         half_size = 1 << stage
         group_size = 2 << stage
 
@@ -130,7 +130,7 @@
             for j in range(half_size):
                 addr0 = group_base + j
                 addr1 = addr0 + half_size
-                twiddle_index = j << (6 - stage)
+                twiddle_index = j << (5 - stage)
 
                 x0_i = int(ram_i[addr0])
                 x0_q = int(ram_q[addr0])
@@ -198,7 +198,7 @@
 
     if not TWIDDLE_RE_PATH.exists() or not TWIDDLE_IM_PATH.exists():
         raise FileNotFoundError(
-            "Generate fft128_twiddle_re.hex and fft128_twiddle_im.hex first"
+            "Generate fft64_twiddle_re.hex and fft64_twiddle_im.hex first"
         )
 
     twiddle_re_rom = load_twiddle_file(TWIDDLE_RE_PATH)
@@ -219,7 +219,7 @@
         numpy_file.write("test,bin,reference_real,reference_imag,rtl_real,rtl_imag\n")
 
         for test_index, (input_i, input_q) in enumerate(tests):
-            output_i, output_q = fft128_rtl_model(
+            output_i, output_q = fft64_rtl_model(
                 input_i,
                 input_q,
                 twiddle_re_rom,
@@ -268,7 +268,7 @@
 
     rms_complex_error = math.sqrt(total_squared_error / total_components)
 
-    print(f"Generated {NUM_TESTS} FFT128 tests")
+    print(f"Generated {NUM_TESTS} FFT64 tests")
     print(f"Input vectors:       {INPUT_PATH}")
     print(f"Bit-accurate golden: {EXPECTED_PATH}")
     print(f"NumPy comparison:    {NUMPY_PATH}")
--- a/golden/gen_ifft128_vectors.py
+++ b/golden/gen_ifft64_vectors.py
@@ -5,18 +5,18 @@
 
 import numpy as np
 
-N = 128
+N = 64
 NUM_TESTS = 5
 SEED = 5678
 FRAC_BITS = 7
 SCALE = 1 << FRAC_BITS
 
 VECTOR_DIR = Path("vectors")
-INPUT_PATH = VECTOR_DIR / "ifft128_inputs.hex"
-EXPECTED_PATH = VECTOR_DIR / "ifft128_expected.hex"
-NUMPY_PATH = VECTOR_DIR / "ifft128_numpy_reference.csv"
-TWIDDLE_RE_PATH = VECTOR_DIR / "fft128_twiddle_re.hex"
-TWIDDLE_IM_PATH = VECTOR_DIR / "fft128_twiddle_im.hex"
+INPUT_PATH = VECTOR_DIR / "ifft64_inputs.hex"
+EXPECTED_PATH = VECTOR_DIR / "ifft64_expected.hex"
+NUMPY_PATH = VECTOR_DIR / "ifft64_numpy_reference.csv"
+TWIDDLE_RE_PATH = VECTOR_DIR / "fft64_twiddle_re.hex"
+TWIDDLE_IM_PATH = VECTOR_DIR / "fft64_twiddle_im.hex"
 
 
 def wrap_signed(value: int, bits: int) -> int:
@@ -35,10 +35,10 @@
     return np.clip(np.round(np.asarray(value) * SCALE), -128, 127).astype(np.int16)
 
 
-def bit_reverse7(value: int) -> int:
+def bit_reverse6(value: int) -> int:
     result = 0
-    for bit in range(7):
-        result |= ((value >> bit) & 1) << (6 - bit)
+    for bit in range(6):
+        result |= ((value >> bit) & 1) << (5 - bit)
     return result
 
 
@@ -51,7 +51,7 @@
                 values.append(wrap_signed(int(token, 16), 8))
 
     if len(values) != N // 2:
-        raise ValueError(f"Expected 64 entries in {path}, found {len(values)}")
+        raise ValueError(f"Expected 32 entries in {path}, found {len(values)}")
 
     return np.asarray(values, dtype=np.int64)
 
@@ -96,25 +96,25 @@
     )
 
 
-def ifft128_rtl_model(
+def ifft64_rtl_model(
     input_i: np.ndarray,
     input_q: np.ndarray,
     twiddle_re_rom: np.ndarray,
     twiddle_im_rom: np.ndarray,
 ) -> tuple[np.ndarray, np.ndarray]:
-    """Mirror the scheduler's bit-reversed-input radix-2 IFFT128 schedule."""
+    """Mirror the scheduler's bit-reversed-input radix-2 IFFT64 schedule."""
     if input_i.shape != (N,) or input_q.shape != (N,):
-        raise ValueError("IFFT128 model requires exactly 128 complex samples")
+        raise ValueError("IFFT64 model requires exactly 64 complex samples")
 
     ram_i = np.zeros(N, dtype=np.int64)
     ram_q = np.zeros(N, dtype=np.int64)
 
     for sample_index in range(N):
-        address = bit_reverse7(sample_index)
+        address = bit_reverse6(sample_index)
         ram_i[address] = wrap_signed(int(input_i[sample_index]), 16)
         ram_q[address] = wrap_signed(int(input_q[sample_index]), 16)
 
-    for stage in range(7):
+    for stage in range(6):
         half_size = 1 << stage
         group_size = 2 << stage
 
@@ -122,7 +122,7 @@
             for j in range(half_size):
                 addr0 = group_base + j
                 addr1 = addr0 + half_size
-                twiddle_index = j << (6 - stage)
+                twiddle_index = j << (5 - stage)
 
                 x0_i = int(ram_i[addr0])
                 x0_q = int(ram_q[addr0])
@@ -160,9 +160,9 @@
                 ram_i[addr1] = X1_i
                 ram_q[addr1] = X1_q
 
-    # Conventional IFFT normalization: divide by 128 after all seven stages.
-    output_i = np.asarray([wrap_signed(int(value) >> 7, 16) for value in ram_i], dtype=np.int16)
-    output_q = np.asarray([wrap_signed(int(value) >> 7, 16) for value in ram_q], dtype=np.int16)
+    # Conventional IFFT normalization: divide by 64 after all six stages.
+    output_i = np.asarray([wrap_signed(int(value) >> 6, 16) for value in ram_i], dtype=np.int16)
+    output_q = np.asarray([wrap_signed(int(value) >> 6, 16) for value in ram_q], dtype=np.int16)
     return output_i, output_q
 
 
@@ -200,7 +200,7 @@
 
     if not TWIDDLE_RE_PATH.exists() or not TWIDDLE_IM_PATH.exists():
         raise FileNotFoundError(
-            "Generate fft128_twiddle_re.hex and fft128_twiddle_im.hex first"
+            "Generate fft64_twiddle_re.hex and fft64_twiddle_im.hex first"
         )
 
     twiddle_re_rom = load_twiddle_file(TWIDDLE_RE_PATH)
@@ -221,7 +221,7 @@
         numpy_file.write("test,sample,reference_real,reference_imag,rtl_real,rtl_imag\n")
 
         for test_index, (input_i, input_q) in enumerate(tests):
-            output_i, output_q = ifft128_rtl_model(
+            output_i, output_q = ifft64_rtl_model(
                 input_i,
                 input_q,
                 twiddle_re_rom,
@@ -268,7 +268,7 @@
 
     rms_complex_error = math.sqrt(total_squared_error / total_bins)
 
-    print(f"Generated {NUM_TESTS} IFFT128 tests")
+    print(f"Generated {NUM_TESTS} IFFT64 tests")
     print(f"Input vectors:       {INPUT_PATH}")
     print(f"Bit-accurate golden: {EXPECTED_PATH}")
     print(f"NumPy comparison:    {NUMPY_PATH}")
--- a/golden/gen_ofdm_rx_vectors.py
+++ b/golden/gen_ofdm_rx_vectors.py
@@ -6,13 +6,13 @@
 
 import numpy as np
 
-N = 128
+N = 64
 FRAC_BITS = 7
 SCALE = 1 << FRAC_BITS
 NUM_TESTS = 4
 SEED = 20260806
-NORMAL_CP = 9
-EXTENDED_CP = 10
+NORMAL_CP = 4
+EXTENDED_CP = 5
 
 VECTOR_DIR = Path("vectors")
 
@@ -29,10 +29,10 @@
     return f"{value & ((1 << bits) - 1):0{digits}x}"
 
 
-def bit_reverse7(value: int) -> int:
+def bit_reverse6(value: int) -> int:
     result = 0
-    for bit in range(7):
-        result |= ((value >> bit) & 1) << (6 - bit)
+    for bit in range(6):
+        result |= ((value >> bit) & 1) << (5 - bit)
     return result
 
 
@@ -46,8 +46,8 @@
         twiddle_im[k] = max(-128, min(127, round(math.sin(angle) * SCALE)))
 
     with (
-        (VECTOR_DIR / "fft128_twiddle_re.hex").open("w", encoding="utf-8") as re_file,
-        (VECTOR_DIR / "fft128_twiddle_im.hex").open("w", encoding="utf-8") as im_file,
+        (VECTOR_DIR / "fft64_twiddle_re.hex").open("w", encoding="utf-8") as re_file,
+        (VECTOR_DIR / "fft64_twiddle_im.hex").open("w", encoding="utf-8") as im_file,
     ):
         for re_value, im_value in zip(twiddle_re, twiddle_im, strict=True):
             re_file.write(hex_signed(int(re_value), 8) + "\n")
@@ -104,7 +104,7 @@
     )
 
 
-def fft128_fixed(
+def fft64_fixed(
     input_i: np.ndarray,
     input_q: np.ndarray,
     twiddle_re_rom: np.ndarray,
@@ -114,11 +114,11 @@
     ram_q = np.zeros(N, dtype=np.int64)
 
     for sample_index in range(N):
-        address = bit_reverse7(sample_index)
+        address = bit_reverse6(sample_index)
         ram_i[address] = wrap_signed(int(input_i[sample_index]), 16)
         ram_q[address] = wrap_signed(int(input_q[sample_index]), 16)
 
-    for stage in range(7):
+    for stage in range(6):
         half_size = 1 << stage
         group_size = 2 << stage
 
@@ -126,7 +126,7 @@
             for j in range(half_size):
                 addr0 = group_base + j
                 addr1 = addr0 + half_size
-                twiddle_index = j << (6 - stage)
+                twiddle_index = j << (5 - stage)
 
                 x0_i = int(ram_i[addr0])
                 x0_q = int(ram_q[addr0])
@@ -231,7 +231,7 @@
                     hex_signed(int(i_code), 8) + hex_signed(int(q_code), 8) + "\n"
                 )
 
-            fft_i, fft_q = fft128_fixed(
+            fft_i, fft_q = fft64_fixed(
                 useful_i,
                 useful_q,
                 twiddle_re,
--- a/golden/gen_ofdm_tx_vectors.py
+++ b/golden/gen_ofdm_tx_vectors.py
@@ -6,14 +6,14 @@
 
 import numpy as np
 
-N = 128
+N = 64
 M = 12
 FRAC_BITS = 7
 SCALE = 1 << FRAC_BITS
 NUM_TESTS = 5
 SEED = 20260806
-NORMAL_CP = 9
-EXTENDED_CP = 10
+NORMAL_CP = 4
+EXTENDED_CP = 5
 SC_START_BIN = 1
 FFT3_COEFF_RE = 0
 FFT3_COEFF_IM = -111
@@ -33,10 +33,10 @@
     return f"{value & ((1 << bits) - 1):0{digits}x}"
 
 
-def bit_reverse7(value: int) -> int:
+def bit_reverse6(value: int) -> int:
     result = 0
-    for bit in range(7):
-        result |= ((value >> bit) & 1) << (6 - bit)
+    for bit in range(6):
+        result |= ((value >> bit) & 1) << (5 - bit)
     return result
 
 
@@ -50,8 +50,8 @@
         twiddle_im[k] = max(-128, min(127, round(math.sin(angle) * SCALE)))
 
     with (
-        (VECTOR_DIR / "fft128_twiddle_re.hex").open("w", encoding="utf-8") as re_file,
-        (VECTOR_DIR / "fft128_twiddle_im.hex").open("w", encoding="utf-8") as im_file,
+        (VECTOR_DIR / "fft64_twiddle_re.hex").open("w", encoding="utf-8") as re_file,
+        (VECTOR_DIR / "fft64_twiddle_im.hex").open("w", encoding="utf-8") as im_file,
     ):
         for re_value, im_value in zip(twiddle_re, twiddle_im, strict=True):
             re_file.write(hex_signed(int(re_value), 8) + "\n")
@@ -233,7 +233,7 @@
     return output_i.astype(np.int16), output_q.astype(np.int16)
 
 
-def ifft128_fixed(
+def ifft64_fixed(
     natural_input_i: np.ndarray,
     natural_input_q: np.ndarray,
     twiddle_re_rom: np.ndarray,
@@ -243,11 +243,11 @@
     ram_q = np.zeros(N, dtype=np.int64)
 
     for sample_index in range(N):
-        address = bit_reverse7(sample_index)
+        address = bit_reverse6(sample_index)
         ram_i[address] = wrap_signed(int(natural_input_i[sample_index]), 16)
         ram_q[address] = wrap_signed(int(natural_input_q[sample_index]), 16)
 
-    for stage in range(7):
+    for stage in range(6):
         half_size = 1 << stage
         group_size = 2 << stage
 
@@ -255,7 +255,7 @@
             for j in range(half_size):
                 addr0 = group_base + j
                 addr1 = addr0 + half_size
-                twiddle_index = j << (6 - stage)
+                twiddle_index = j << (5 - stage)
 
                 x0_i = int(ram_i[addr0])
                 x0_q = int(ram_q[addr0])
@@ -286,10 +286,10 @@
                 ram_q[addr1] = X1_q
 
     output_i = np.asarray(
-        [wrap_signed(int(value) >> 7, 16) for value in ram_i], dtype=np.int16
+        [wrap_signed(int(value) >> 6, 16) for value in ram_i], dtype=np.int16
     )
     output_q = np.asarray(
-        [wrap_signed(int(value) >> 7, 16) for value in ram_q], dtype=np.int16
+        [wrap_signed(int(value) >> 6, 16) for value in ram_q], dtype=np.int16
     )
     return output_i, output_q
 
@@ -359,7 +359,7 @@
             grid_i[SC_START_BIN:SC_START_BIN + M] = dft_i
             grid_q[SC_START_BIN:SC_START_BIN + M] = dft_q
 
-            time_i, time_q = ifft128_fixed(grid_i, grid_q, twiddle_re, twiddle_im)
+            time_i, time_q = ifft64_fixed(grid_i, grid_q, twiddle_re, twiddle_im)
             output_indices = list(range(N - cp_length, N)) + list(range(N))
 
             ideal_input = (
