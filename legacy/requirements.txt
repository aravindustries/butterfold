numpy>=1.24.0
python-dotenv>=1.0.0

# Agentic orchestration (agents/orchestrator.py, agents/rtl_react_agent.py).
# The whole pipeline runs without these too (deterministic fallback path);
# they're only needed to run the LangGraph state graph / ReAct code agent.
langgraph>=1.0.0
langchain-core>=1.0.0
# ChatOpenAI: used for BOTH the "openai" provider and the "openweight" provider
# (any OpenAI-compatible endpoint: Ollama, vLLM, llama.cpp server, LM Studio, ...).
langchain-openai>=0.2.0
