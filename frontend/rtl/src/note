need a module to compute the prime factors of a given number & store the value in an array
so if there's a radix 2 & radix 3 buttefly only then the array will have 2 elements & the value of each element will be the number 
of stages that butterfly needs to have


write a python program to generate twiddle values
