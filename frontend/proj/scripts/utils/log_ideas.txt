Setup a list for whitelisting loggers in the json file
The log_file.log must be named after the script's main file
	If a file with that name already exists then it should append a (1) to it and so on
Be able to configure multiple debuggers with their own verbosity levels & output files(component name by default) as -d PARSER,INFO,File -d PATHER,DEBUG,INFO,file
Create an optional file for the logger which will be named after the component that was whitelisted
Create an optional json format of the log files also for easier parsing

